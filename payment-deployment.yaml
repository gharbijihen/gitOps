apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payments
  template:
    metadata:
      labels:
        app: payments
    spec:
      imagePullSecrets:
        - name: nexus-creds
      containers:
        - name: payments
          image: jihengharbi/jihen-payments:latest
          ports:
            - containerPort: 8080

